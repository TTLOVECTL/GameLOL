using System.Collections;
using System.Collections.Generic;

namespace EquipmentSystem {

    public enum EqunipmentType {
        SMALL,
        MIDDLE,
        BIG
    }

    public enum SkillType {
        PASSIVE,
        INITIATIVE
    }

    public enum SearchType
    {
        ATTACK,
        MEGIC,
        DEFENSE,
        MOVE,
        DAYE,
        FUZHU
    }
}
